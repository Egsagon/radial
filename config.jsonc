// ------------------ //
// Radial config file //
// ------------------ //

{
    // Aternos credentials used to connect. Directly passed into the aternos client.
    "aternos": {
        "username": "",
        "password": ""
    },

    // The Discord bot token (needs all intents)
    "token": "",

    // The server id
    "server_id": null
}